# v1.7.0-luoluo.2：源码快照、定位与回退

日期：2026-09-21。用户授权合并到 `main` 并编号，方便回退和排查 bug。本记录用于源码追踪；没有创建 GitHub Release，没有生产部署，也不作为完整发布验收报告。

## 版本对应

| 项目 | 固定标识 |
| --- | --- |
| Kiwi 二改标签 | `v1.7.0-luoluo.2`（附注标签，指向本次 main 合并提交） |
| 原版基线 | `LucieEveille/kiwi-mem v1.7.0` |
| 原版准确提交 | `b01a0c506f4f10f90f30d408c0291f16720f0718`（保持不变） |
| 旧二改标签 | `v1.7.0-luoluo.1` |
| 旧二改准确提交 | `51ed818a03ae9e85e4c9891bc3c5f1f1c852e425` |
| 本次功能与回归基线 | `16772c0f7caab98993c69de114db37114dc266e4` |
| Kiwi Dream 修复源码 | `91737952c47ac140dddd2802f632702f3687378f` |
| 配套 float | `v0.2.0` / `77530ebaeb46bfbc499079cb8148ce38c2deed11` |

标签最终提交通过以下命令读取，避免在文件里嵌入包含该文件自身的提交哈希。旧标签不移动，不覆盖。

```powershell
git fetch origin --tags
git rev-parse 'v1.7.0-luoluo.2^{commit}'
git show --no-patch v1.7.0-luoluo.2
git log --oneline v1.7.0-luoluo.1..v1.7.0-luoluo.2
git diff --stat v1.7.0-luoluo.1 v1.7.0-luoluo.2
```

## 变更与验证边界

- 在原有角色独立数据库和 worker 上增加 `float-memory-v1`：事件幂等写入、全局/群聊范围召回；删除记忆保留收据，避免重试复活。
- 群事件原文不参与 Dream 转换与软化；自动 Dream 活动判断加入服务端 float 事件时间，重复事件不刷新活动。
- 群派生记忆来源追踪尚未实现，原文保护不恢复旧版本已消化的数据。
- [适配说明](../float-memory-adapter.md) 保存功能提交的本地 HTTP/真实临时 PostgreSQL、模拟供应商、回归及负向变异证据，不能当作真实供应商或生产证明。
- 本次编号只修改版本常量和说明，服务仍名为 Kiwi-Mem；版本烟雾检查验证常量、主服务/监督服务 OpenAPI 与状态接口的标识。GitHub CI 结果以 [PR #2 Checks](https://github.com/luoluo-test/kiwi-mem/pull/2/checks) 和对应提交的 Actions 为准。
- 2026-09-21 编号检查：版本/基线常量、服务名、两个 OpenAPI 版本及 ASGI `GET /` 均通过（模拟配置，不启动数据库或真实供应商）；`python -m compileall -q .`、`python scripts/test_character_isolation.py`（22 tests）及 `git diff --check` 通过。OpenAPI 保留原有代理路由重复 operation ID 警告，不影响本次版本标识。
- 真实模型、生产容量、设备端与完整多库恢复未完成。ZIP 导入的项目归属缺口仍需处理，不使用 ZIP 进行生产迁移。

## 排查 bug 时记录

记录 `git rev-parse HEAD`、`git describe --tags --always --dirty`、状态接口中的 `fork_version` / `upstream_commit`，以及配套 float 版本、复现步骤和脱敏错误。运行时版本为 `1.7.0-luoluo.2`；不要附带密钥、完整私聊或数据库连接串。

## 回退到旧二改

1. 暂停写入，独立备份角色注册表与所有角色数据库，验证备份可读取。备份必须包含 `float_memory_events`；普通 Kiwi ZIP 不包含完整收据，不能替代数据库备份。
2. 旧 `v1.7.0-luoluo.1` 没有 float API。先把 float 切回内置记忆并同步微信运行包；这不会自动把 Kiwi 记忆搬回 float。
3. 在独立目录检出旧代码，先用数据库副本验证。以下操作只创建代码工作树，不改写当前分支历史：

   ```powershell
   git fetch origin --tags
   git worktree add --detach ../kiwi-mem-rollback-v1.7.0-luoluo.1 v1.7.0-luoluo.1
   ```

4. 确认兼容后再安排服务切换。保留全部角色库、注册表和事件收据，不删除表来回退，也不让新旧服务同时写同一库。生产切换和备份恢复需单独验证，本次未执行。

若只需回到本次新快照，以同样方式在新目录检出 `v1.7.0-luoluo.2`。修复 bug 时从标签建立新分支，保留原标签作为复现依据。
